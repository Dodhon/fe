[package]
name = "fe"
version = "0.1.0"
edition = "2021"
description = "Cached command-line wrapper for QMD retrieval."
repository = "https://github.com/Dodhon/fe"
publish = false

[dependencies]
rusqlite = { version = "0.40.1", features = ["bundled"] }
